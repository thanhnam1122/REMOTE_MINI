<Window x:Class="RemoteDesktopClient.UI.YellowBorderOverlay"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="YellowBorderOverlay"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        WindowStartupLocation="Manual"
        Left="0" Top="0"
        Focusable="False">

    <Grid>
        <!-- Outer Glowing Yellow Border -->
        <Border BorderBrush="#F59E0B" BorderThickness="3.5" Margin="0" CornerRadius="0">
            <Border.Effect>
                <DropShadowEffect Color="#F59E0B" BlurRadius="12" ShadowDepth="0" Opacity="0.85"/>
            </Border.Effect>
        </Border>

        <!-- Top Badge Banner -->
        <Border Background="#EAB308" CornerRadius="0,0,8,8" HorizontalAlignment="Center" VerticalAlignment="Top" Padding="12,4" Margin="0,0,0,0">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="🟡 ĐANG CHIA SẺ &amp; ĐIỀU KHIỂN MÀN HÌNH" FontSize="11" FontWeight="Bold" Foreground="#0F1117" VerticalAlignment="Center"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
